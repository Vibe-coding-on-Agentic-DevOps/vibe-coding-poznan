# Azure Developer CLI configuration for your project
name: vibe-coding-poz-wro
metadata:
  template: custom
services:
  backend:
    project: ./workspace/backend
    language: py
    host: appservice
  frontend:
    project: ./workspace/frontend
    language: js
    host: staticwebapp
